<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tinyhr.organization.adapter.mapper.DepartmentQueryMapper">

    <select id="listAll"
            resultType="com.example.tinyhr.organization.adapter.mapper.DepartmentListItem">
        SELECT
            department_id    AS departmentId,
            name             AS name,
            parent_id        AS parentId,
            depth            AS depth,
            is_active        AS active,
            display_order    AS displayOrder,
            responsibilities AS responsibilities
        FROM department
        ORDER BY depth ASC, display_order ASC, name ASC
    </select>

</mapper>
